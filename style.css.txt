/* 全局样式 */
body {
    margin: 0;
    padding: 0;
    background: #f6f3eb;
    font-family: "Arial", sans-serif;
    color: #333;
}

/* 顶部导航栏 */
.navbar {
    width: 100%;
    padding: 16px 20px;
    background: #fff;
    border-bottom: 2px solid #eee;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: sticky;
    top: 0;
}

.nav-title {
    font-size: 20px;
    font-weight: bold;
}

.incense-count {
    font-size: 16px;
    background: #ffd58a;
    padding: 6px 14px;
    border-radius: 20px;
    font-weight: bold;
}

/* 考神选择区块 */
.container {
    padding: 20px;
}

.god-card {
    background: #fff;
    border-radius: 12px;
    padding: 20px;
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    box-shadow: 0 4px 10px rgba(0,0,0,0.05);
}

.god-icon {
    width: 60px;
    height: 60px;
    background: #ececec;
    border-radius: 50%;
    margin-right: 16px;
}

.god-info {
    flex: 1;
}

.god-name {
    font-size: 18px;
    font-weight: bold;
}

.btn-bai {
    padding: 10px 16px;
    background: #ffb347;
    color: #fff;
    font-weight: bold;
    border-radius: 8px;
    cursor: pointer;
}

/* 拜神动画（烟雾 + 光效） */
.bai-animation {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    display: none;
}

.smoke {
    width: 120px;
    height: 120px;
    background: radial-gradient(circle, rgba(255,255,255,0.8), transparent);
    border-radius: 50%;
    animation: smokeMove 1.5s infinite ease-in-out;
}

@keyframes smokeMove {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(-40px); opacity: 0; }
}

/* 奖励弹窗 */
.reward-popup {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: #fff5da;
    padding: 28px;
    border-radius: 12px;
    text-align: center;
    width: 260px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    display: none;
}

.reward-title {
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 12px;
}

.reward-content {
    font-size: 16px;
    margin-bottom: 20px;
}

.btn-confirm {
    padding: 8px 16px;
    background: #ffb347;
    color: #fff;
    border-radius: 8px;
    cursor: pointer;
    font-weight: bold;
}